---
phase: 04-Spec-Drift-Management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/core/drift.ts
  - packages/cli/src/core/types.ts
  - packages/cli/src/core/frontmatter.ts
  - packages/cli/src/core/init.ts
  - packages/cli/src/core/index.ts
  - packages/cli/src/cli.ts
autonomous: true
requirements:
  - DRIFT-01
  - DRIFT-02
must_haves:
  truths:
    - "CLI tools router responds to 'drift' subcommands without errors"
    - "'init check-drift' returns a valid CheckDriftContext JSON object"
    - "Frontmatter validation accepts 'drift' as a valid schema name"
    - "DriftReport types are exported from @maxsim/core index"
  artifacts:
    - path: "packages/cli/src/core/drift.ts"
      provides: "Drift report read/write, requirement extraction, no-go extraction, convention extraction, previous hash retrieval"
      exports: ["cmdDriftReadReport", "cmdDriftWriteReport", "cmdDriftExtractRequirements", "cmdDriftExtractNoGos", "cmdDriftExtractConventions", "cmdDriftPreviousHash"]
    - path: "packages/cli/src/core/types.ts"
      provides: "DriftReportFrontmatter, DriftStatus, DriftSeverity, DriftDirection, DriftItemStatus, CheckDriftContext, RealignContext type definitions"
    - path: "packages/cli/src/core/frontmatter.ts"
      provides: "drift schema in FRONTMATTER_SCHEMAS"
    - path: "packages/cli/src/core/init.ts"
      provides: "cmdInitCheckDrift and cmdInitRealign context assembly functions"
    - path: "packages/cli/src/cli.ts"
      provides: "drift command dispatch and init check-drift/realign dispatch"
  key_links:
    - from: "packages/cli/src/cli.ts"
      to: "packages/cli/src/core/drift.ts"
      via: "import and COMMANDS['drift'] handler dispatch"
      pattern: "'drift'.*handleDrift"
    - from: "packages/cli/src/cli.ts"
      to: "packages/cli/src/core/init.ts"
      via: "handleInit dispatch for check-drift and realign"
      pattern: "'check-drift'.*cmdInitCheckDrift"
    - from: "packages/cli/src/core/index.ts"
      to: "packages/cli/src/core/drift.ts"
      via: "re-export of drift commands and types"
      pattern: "from.*drift"
---

<objective>
Create the core drift module, TypeScript types, frontmatter schema, init context commands, and CLI dispatch wiring that the check-drift agent and realign workflow will consume. This is the foundation layer -- all drift-related tool commands that agents call via the Bash tool.
</objective>

<execution_context>
Phase 4 adds spec drift detection to MAXSIM. This plan creates the backend infrastructure: a new `drift.ts` core module with CLI tool commands, type definitions, frontmatter schema, and init context assembly. Plans 02 and 03 (Wave 2) will create the agent prompts, commands, and workflows that use these tools.

The user locked these decisions:
- Output to `.planning/DRIFT-REPORT.md`
- YAML frontmatter with status/critical_count/warning_count/info_count
- Three severity tiers: critical/warning/info
- Full `.planning/` sweep including archived milestones
- Fresh codebase scan every run
- Timestamped with diff tracking (previous report hash)
</execution_context>

<context>
@packages/cli/src/core/types.ts (existing type definitions -- add new types here)
@packages/cli/src/core/frontmatter.ts (FRONTMATTER_SCHEMAS record -- add drift schema)
@packages/cli/src/core/init.ts (init command pattern -- add cmdInitCheckDrift, cmdInitRealign)
@packages/cli/src/core/index.ts (barrel exports -- add drift exports)
@packages/cli/src/cli.ts (COMMANDS record -- add drift handler and init dispatch)
@packages/cli/src/core/core.ts (planningPath, phasesPath, listSubDirs, pathExistsInternal, loadConfig, resolveModelInternal, findPhaseInternal, getArchivedPhaseDirs, safeReadFile, writeOutput)
@.planning/phases/04-Spec-Drift-Management/04-RESEARCH.md (verified patterns, code examples)
</context>

<tasks>

<task id="01" type="auto">
<name>Drift types, frontmatter schema, and core module</name>
<files>
- packages/cli/src/core/types.ts
- packages/cli/src/core/frontmatter.ts
- packages/cli/src/core/drift.ts
</files>
<action>
**1. Add types to `types.ts`** (append after the existing `DebuggerAgentContext` interface):

Add these types following the exact pattern from the research:
- `DriftStatus = 'drift' | 'aligned'`
- `DriftSeverity = 'critical' | 'warning' | 'info'`
- `DriftDirection = 'spec_ahead' | 'code_ahead' | 'undocumented'`
- `DriftItemStatus = 'NEW' | 'RESOLVED' | 'UNCHANGED'`
- `DriftReportFrontmatter` interface with fields: status (DriftStatus), checked (string, ISO timestamp), previous_hash (string | null), previous_report_date (string | null), total_items (number), aligned_count (number), critical_count (number), warning_count (number), info_count (number), undocumented_count (number), spec_files_checked (string[])
- `CheckDriftContext` interface with fields: drift_model (string), commit_docs (boolean), has_planning (boolean), has_requirements (boolean), has_roadmap (boolean), has_nogos (boolean), has_conventions (boolean), has_previous_report (boolean), previous_report_path (string | null), spec_files (string[]), phase_dirs (string[]), archived_milestone_dirs (string[]), state_path (string), requirements_path (string), roadmap_path (string), nogos_path (string | null), conventions_path (string | null), codebase_docs (string[])
- `RealignContext` interface with fields: commit_docs (boolean), direction (string | null), has_report (boolean), report_path (string), has_planning (boolean), state_path (string), roadmap_path (string), requirements_path (string), phase_dirs (string[]), codebase_docs (string[])
- Add `'maxsim-drift-checker'` to the `AgentType` union

**2. Add drift schema to `frontmatter.ts`** in the `FRONTMATTER_SCHEMAS` record:
```
drift: {
  required: ['status', 'checked', 'total_items', 'critical_count', 'warning_count', 'info_count'],
},
```

**3. Create `packages/cli/src/core/drift.ts`** -- new module following the patterns in `verify.ts` and `roadmap.ts`:

Import from `./core.js`: planningPath, phasesPath, safeReadFile, listSubDirs, pathExistsInternal, writeOutput, getArchivedPhaseDirs
Import from `./types.js`: cmdOk, cmdErr, CmdResult
Import from `./frontmatter.js`: extractFrontmatter
Import fs and path from node:

Implement these functions:

- `cmdDriftReadReport(cwd: string): CmdResult` -- Read `.planning/DRIFT-REPORT.md`, parse frontmatter, return {frontmatter, content} or error if not found
- `cmdDriftWriteReport(cwd: string, content: string | null, contentFile: string | null): CmdResult` -- Write content to `.planning/DRIFT-REPORT.md`. If contentFile provided, read from that file (supports the tmpfile pattern for large reports). Use writeOutput pattern for large content. Return {written: true, path: '.planning/DRIFT-REPORT.md'}
- `cmdDriftExtractRequirements(cwd: string): CmdResult` -- Read `.planning/REQUIREMENTS.md`, parse all requirement lines (pattern: `- [ ] **ID**: description` or `- [x] **ID**: description`), return array of {id, description, complete (boolean), line_number}
- `cmdDriftExtractNoGos(cwd: string): CmdResult` -- Read `.planning/NO-GOS.md` if it exists. Parse no-go items (bullet points in sections). Return array of {rule, section, line_number} or empty array if file missing
- `cmdDriftExtractConventions(cwd: string): CmdResult` -- Read `.planning/CONVENTIONS.md` if it exists. Return the full content as a string for the agent to analyze, or null if missing
- `cmdDriftPreviousHash(cwd: string): CmdResult` -- Read existing DRIFT-REPORT.md frontmatter, extract `previous_hash` and `checked` fields. Return {hash, checked_date} or null if no report exists

Each function should follow the existing cmdOk/cmdErr pattern. Handle missing files gracefully (return structured data indicating absence, do NOT throw).
</action>
<verify>
<automated>cd C:/Development/cli/maxsim && npx tsc --noEmit --project packages/cli/tsconfig.json 2>&1 | head -30</automated>
</verify>
<done>
- drift.ts exists with 6 exported cmd* functions
- types.ts has DriftReportFrontmatter, CheckDriftContext, RealignContext interfaces and DriftStatus/DriftSeverity/DriftDirection/DriftItemStatus type aliases
- types.ts AgentType union includes 'maxsim-drift-checker'
- frontmatter.ts FRONTMATTER_SCHEMAS has a 'drift' entry with required fields
- TypeScript compiles without errors
</done>
</task>

<task id="02" type="auto">
<name>Init context commands and CLI dispatch wiring</name>
<files>
- packages/cli/src/core/init.ts
- packages/cli/src/core/index.ts
- packages/cli/src/cli.ts
</files>
<action>
**1. Add init commands to `init.ts`:**

Add `CheckDriftContext` and `RealignContext` to the type imports from `./types.js`.

Add `cmdInitCheckDrift(cwd: string): CmdResult` function following the exact pattern of `cmdInitVerifyWork`:
- Load config via `loadConfig(cwd)`
- Resolve model: `resolveModelInternal(cwd, 'maxsim-drift-checker')` -- this will fall back to the verifier tier since drift-checker is new
- Check existence of: `.planning/`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/NO-GOS.md`, `.planning/CONVENTIONS.md`, `.planning/DRIFT-REPORT.md`, `.planning/STATE.md`
- Collect spec_files: list all existing spec file paths (REQUIREMENTS.md, ROADMAP.md, STATE.md, NO-GOS.md, CONVENTIONS.md)
- Collect phase_dirs: use `listSubDirs(phasesPath(cwd))` to get active phase directories, prefix each with `.planning/phases/`
- Collect archived_milestone_dirs: use `getArchivedPhaseDirs(cwd)` -- if this returns ArchivedPhaseDir[], map to their basePath values. If function is not directly suitable, use `listSubDirs(planningPath(cwd, 'archive'))` with error handling
- Get codebase docs via `listCodebaseDocs(cwd)`
- Return CheckDriftContext via cmdOk

Add `cmdInitRealign(cwd: string, direction: string | undefined): CmdResult` function:
- Load config
- Check if DRIFT-REPORT.md exists
- Collect phase_dirs and codebase_docs same as above
- Return RealignContext with direction (from arg or null), report_path, spec file paths, codebase_docs

Add `'check-drift'` and `'realign'` to the `WorkflowType` union.

**2. Update `index.ts`** barrel exports:
- Add type exports: `CheckDriftContext`, `RealignContext`, `DriftReportFrontmatter`, `DriftStatus`, `DriftSeverity`, `DriftDirection`, `DriftItemStatus` from `./types.js`
- Add runtime exports from `./drift.js`: `cmdDriftReadReport`, `cmdDriftWriteReport`, `cmdDriftExtractRequirements`, `cmdDriftExtractNoGos`, `cmdDriftExtractConventions`, `cmdDriftPreviousHash`
- Add init exports: `cmdInitCheckDrift`, `cmdInitRealign` from `./init.js`

**3. Wire into `cli.ts`:**

Add imports at the top: `cmdDriftReadReport`, `cmdDriftWriteReport`, `cmdDriftExtractRequirements`, `cmdDriftExtractNoGos`, `cmdDriftExtractConventions`, `cmdDriftPreviousHash`, `cmdInitCheckDrift`, `cmdInitRealign`

Add `handleDrift` handler (before COMMANDS record) following the exact pattern of `handleVerify`:
```typescript
const handleDrift: Handler = (args, cwd, raw) => {
  const sub = args[1];
  const handlers: Record<string, () => CmdResult | Promise<CmdResult>> = {
    'read-report': () => cmdDriftReadReport(cwd),
    'extract-requirements': () => cmdDriftExtractRequirements(cwd),
    'extract-nogos': () => cmdDriftExtractNoGos(cwd),
    'extract-conventions': () => cmdDriftExtractConventions(cwd),
    'write-report': () => cmdDriftWriteReport(cwd, getFlag(args, '--content'), getFlag(args, '--content-file')),
    'previous-hash': () => cmdDriftPreviousHash(cwd),
  };
  const handler = sub ? handlers[sub] : undefined;
  if (handler) return handleResult(handler(), raw);
  error('Unknown drift subcommand. Available: read-report, extract-requirements, extract-nogos, extract-conventions, write-report, previous-hash');
};
```

Add `'drift': handleDrift` to the COMMANDS record.

Add to the `handleInit` handlers record:
```typescript
'check-drift': () => cmdInitCheckDrift(cwd),
'realign': () => cmdInitRealign(cwd, args[2]),
```

Update the error message string in handleInit to include `check-drift, realign` in the available list.
</action>
<verify>
<automated>cd C:/Development/cli/maxsim && npx tsc --noEmit --project packages/cli/tsconfig.json 2>&1 | head -30 && echo "---BUILD CHECK---" && npm run build:cli 2>&1 | tail -5</automated>
</verify>
<done>
- `node dist/cli.cjs drift read-report` runs without crash (returns error about missing report, which is correct)
- `node dist/cli.cjs drift extract-requirements` returns parsed requirements array
- `node dist/cli.cjs init check-drift` returns a CheckDriftContext JSON object with all expected fields
- `node dist/cli.cjs init realign to-code` returns a RealignContext JSON object
- TypeScript compiles without errors
- CLI builds successfully
</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd packages/cli && npx tsc --noEmit` -- zero type errors
2. `npm run build:cli` -- builds without errors
3. `node dist/cli.cjs drift extract-requirements` -- returns valid JSON with requirement entries
4. `node dist/cli.cjs init check-drift` -- returns CheckDriftContext with has_planning, has_requirements, spec_files, etc.
5. `node dist/cli.cjs drift read-report` -- returns structured "not found" result (no report exists yet)
6. `node dist/cli.cjs init realign` -- returns RealignContext
</verification>

<success_criteria>
- New `drift.ts` core module exists with 6 cmd* functions for drift report CRUD and spec extraction
- TypeScript types for drift detection (DriftReportFrontmatter, CheckDriftContext, RealignContext, severity/status unions) are defined and exported
- Frontmatter schema 'drift' is registered for validation
- CLI tools router responds to `drift` subcommands and `init check-drift`/`init realign`
- All functions handle missing files gracefully (no crashes on projects without .planning/)
- Build succeeds and all existing tests still pass
</success_criteria>

<output>
Updated files: drift.ts (new), types.ts, frontmatter.ts, init.ts, index.ts, cli.ts
New CLI commands: drift read-report, drift extract-requirements, drift extract-nogos, drift extract-conventions, drift write-report, drift previous-hash, init check-drift, init realign
New types exported: DriftReportFrontmatter, CheckDriftContext, RealignContext, DriftStatus, DriftSeverity, DriftDirection, DriftItemStatus
</output>
