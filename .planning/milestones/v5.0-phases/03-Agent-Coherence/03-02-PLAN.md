---
phase: 03-Agent-Coherence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/agents/maxsim-verifier.md
  - templates/agents/maxsim-spec-reviewer.md
  - templates/agents/maxsim-code-reviewer.md
  - templates/agents/maxsim-debugger.md
  - templates/agents/maxsim-codebase-mapper.md
  - templates/agents/maxsim-integration-checker.md
  - templates/agents/AGENTS.md
autonomous: true
requirements:
  - AGENT-01
  - AGENT-04
must_haves:
  truths:
    - "Each of the 6 support agents has a system map listing all 13 agents"
    - "Each of the 6 support agents has upstream_input and downstream_consumer sections"
    - "Each of the 6 support agents validates required inputs and returns structured error on missing"
    - "Each of the 6 support agents has deferred items protocol section"
    - "Each of the 6 support agents' structured returns include minimum handoff contract"
    - "Each of the 6 support agents has needs field in YAML frontmatter"
    - "AGENTS.md documents the system map update convention for new agents"
  artifacts:
    - path: "templates/agents/maxsim-verifier.md"
      provides: "Verifier agent with system map, contracts, validation, deferred items, needs frontmatter"
      min_lines: 150
    - path: "templates/agents/maxsim-spec-reviewer.md"
      provides: "Spec reviewer agent with system map, contracts, validation, deferred items, needs frontmatter"
      min_lines: 120
    - path: "templates/agents/maxsim-code-reviewer.md"
      provides: "Code reviewer agent with system map, contracts, validation, deferred items, needs frontmatter"
      min_lines: 120
    - path: "templates/agents/maxsim-debugger.md"
      provides: "Debugger agent with system map, contracts, validation, deferred items, needs frontmatter"
      min_lines: 150
    - path: "templates/agents/maxsim-codebase-mapper.md"
      provides: "Codebase mapper agent with system map, contracts, validation, deferred items, needs frontmatter"
      min_lines: 100
    - path: "templates/agents/maxsim-integration-checker.md"
      provides: "Integration checker agent with system map, contracts, validation, deferred items, needs frontmatter"
      min_lines: 100
    - path: "templates/agents/AGENTS.md"
      provides: "Updated registry with new-agent convention for system map updates"
      min_lines: 50
  key_links:
    - from: "templates/agents/maxsim-spec-reviewer.md"
      to: "templates/agents/maxsim-executor.md"
      via: "upstream_input declares inline context from executor"
      pattern: "inline.*executor|Receives from.*executor"
    - from: "templates/agents/maxsim-code-reviewer.md"
      to: "templates/agents/maxsim-executor.md"
      via: "upstream_input declares inline context from executor"
      pattern: "inline.*executor|Receives from.*executor"
    - from: "templates/agents/maxsim-verifier.md"
      to: "templates/agents/maxsim-executor.md"
      via: "upstream_input references execute-phase orchestrator"
      pattern: "execute-phase|VERIFICATION.md"
---

<objective>
Add agent coherence sections to the 6 support agents: verifier, spec-reviewer, code-reviewer, debugger, codebase-mapper, and integration-checker. Also update AGENTS.md registry with the convention that adding a new agent requires updating the system map in all existing agents. Each agent gets the same treatment as Plan 01: needs frontmatter, system map, upstream/downstream contracts, input validation, deferred items, and minimum handoff contract.
</objective>

<execution_context>
@.planning/phases/03-Agent-Coherence/03-CONTEXT.md
@.planning/phases/03-Agent-Coherence/03-RESEARCH.md
</execution_context>

<context>
This plan runs in parallel with Plan 01 (Wave 1). Plan 01 handles the 7 core agents, this plan handles the remaining 6 support agents plus the AGENTS.md registry.

**Key context (same as Plan 01):**
- Pattern 1 from RESEARCH.md defines section ordering
- Code Example 1 provides the exact 13-agent system map table
- Code Example 6 provides the input validation block template
- Pattern 5 defines the minimum handoff contract format

**Important distinction for reviewers (spec-reviewer, code-reviewer):**
Per CONTEXT.md locked decision: "Formalized inline context as checklist. For agents spawned with inline context (spec-reviewer, code-reviewer), the executor prompt includes a checklist of what must be included when spawning each reviewer."
- These agents declare `needs: [inline]` in frontmatter (all context is passed inline by the executor)
- Their upstream_input section documents what the executor must include when spawning them
- They do NOT have init commands (context comes inline)

**System map (identical for all agents -- same as Plan 01):**
```markdown
<agent_system_map>
## Agent System Map

| Agent | Role |
|-------|------|
| maxsim-executor | Implements plan tasks with atomic commits and deviation handling |
| maxsim-planner | Creates executable phase plans with goal-backward verification |
| maxsim-plan-checker | Verifies plans achieve phase goal before execution |
| maxsim-phase-researcher | Researches phase domain for planning context |
| maxsim-project-researcher | Researches project ecosystem during init |
| maxsim-research-synthesizer | Synthesizes parallel research into unified findings |
| maxsim-roadmapper | Creates roadmaps with phase breakdown and requirement mapping |
| maxsim-verifier | Verifies phase goal achievement with fresh evidence |
| maxsim-spec-reviewer | Reviews implementation for spec compliance |
| maxsim-code-reviewer | Reviews implementation for code quality |
| maxsim-debugger | Investigates bugs via systematic hypothesis testing |
| maxsim-codebase-mapper | Maps codebase structure and conventions |
| maxsim-integration-checker | Validates cross-component integration |
</agent_system_map>
```

**Locked decisions (NON-NEGOTIABLE):**
- System map inlined in every agent (NOT a shared partial file)
- Spec-reviewer and code-reviewer receive ALL context inline from executor
- Hard validation blocks execution on missing critical input
- All agents log deferred items in standardized format
- Minimum handoff contract: decisions + artifacts + status + deferred items
</context>

<tasks>

<task id="1" type="auto">
<name>Add coherence sections to verifier, spec-reviewer, code-reviewer</name>
<files>
- templates/agents/maxsim-verifier.md
- templates/agents/maxsim-spec-reviewer.md
- templates/agents/maxsim-code-reviewer.md
</files>
<action>
For each of the 3 agent files, read fully then add coherence sections. Follow the same section ordering as Plan 01.

**1. Add `needs` to YAML frontmatter:**
- maxsim-verifier: `needs: [phase_dir, roadmap, state, requirements, codebase_docs]`
- maxsim-spec-reviewer: `needs: [inline]` (all context passed inline by executor)
- maxsim-code-reviewer: `needs: [inline]` (all context passed inline by executor)

**2. Add `<agent_system_map>` AFTER frontmatter, BEFORE `<role>`:**
Use the exact 13-agent table from context above.

**3. Add `<upstream_input>` AFTER `</role>`:**
- **maxsim-verifier:** Receives from execute-phase orchestrator. Inputs: phase directory path (required), phase goal from ROADMAP.md (required), phase requirement IDs (required), PLAN.md must_haves (required). Reference: "See PLAN.md frontmatter must_haves for verification targets". Validation: block if phase directory or phase goal missing.
- **maxsim-spec-reviewer:** Receives from maxsim-executor (inline). Inputs: task specs with action/done criteria/files (inline, required), modified files list from git diff (inline, required), plan frontmatter requirements (inline, required). Note: "All context is passed inline. This agent does NOT read plan files directly. The executor is responsible for providing complete context." Validation: block if task specs or modified files list missing.
- **maxsim-code-reviewer:** Receives from maxsim-executor (inline). Inputs: modified files list from git diff (inline, required), CONVENTIONS.md content or summary (inline, optional), test results (inline, optional). Note: "All context is passed inline. This agent does NOT read plan files directly." Validation: block if modified files list missing.

**4. Add `<downstream_consumer>` AFTER `</upstream_input>`:**
- **maxsim-verifier:** Produces for execute-phase orchestrator (via file). Outputs: VERIFICATION.md (file, durable). Contains: truth verification results, gap analysis, score, status (passed/human_needed/gaps_found).
- **maxsim-spec-reviewer:** Produces for maxsim-executor (inline). Outputs: review verdict with frontmatter (inline, ephemeral). Contains: status (PASS/FAIL), critical_count, warning_count, per-requirement findings. Format: YAML frontmatter + markdown body (per CONTEXT.md locked decision on review output format).
- **maxsim-code-reviewer:** Produces for maxsim-executor (inline). Outputs: review verdict with frontmatter (inline, ephemeral). Contains: status (PASS/FAIL), critical_count, warning_count, code quality findings. Format: YAML frontmatter + markdown body.

**5. Add `<input_validation>` AFTER `</downstream_consumer>`:**
Customize per agent. For spec-reviewer and code-reviewer, validation checks that inline context was provided (task specs present, files list present). Use the structured error format from Code Example 6.

**6. Add `<deferred_items>` protocol section near end.**

**7. Update `<structured_returns>` to include minimum handoff contract.**
For spec-reviewer and code-reviewer: the review verdict output should include frontmatter (status, critical_count, warning_count) followed by the handoff contract sections. This is a key change -- review output must be machine-parseable via `extractFrontmatter()`.

IMPORTANT: Do NOT change existing review logic or spec compliance checking in reviewers. Only ADD the new sections. The existing `<review_process>` sections in spec-reviewer and code-reviewer remain unchanged.
</action>
<verify>
<automated>cd C:/Development/cli/maxsim && for f in templates/agents/maxsim-verifier.md templates/agents/maxsim-spec-reviewer.md templates/agents/maxsim-code-reviewer.md; do echo "=== $f ===" && grep -c "agent_system_map" "$f" && grep -c "upstream_input" "$f" && grep -c "downstream_consumer" "$f" && grep -c "input_validation" "$f" && grep -c "deferred_items" "$f" && grep -c "needs:" "$f" && grep -c "Key Decisions\|status.*PASS\|critical_count" "$f"; done</automated>
</verify>
<done>
- All 3 agents have system map, upstream_input, downstream_consumer, input_validation, deferred_items sections
- All 3 agents have `needs:` in YAML frontmatter
- spec-reviewer and code-reviewer document frontmatter output format (status, critical_count, warning_count)
- verifier documents VERIFICATION.md as output
- No existing review logic was changed
</done>
</task>

<task id="2" type="auto">
<files>
<name>Add coherence sections to debugger, codebase-mapper, integration-checker plus AGENTS.md</name>
- templates/agents/maxsim-debugger.md
- templates/agents/maxsim-codebase-mapper.md
- templates/agents/maxsim-integration-checker.md
- templates/agents/AGENTS.md
</files>
<action>
**Part A: Update 3 remaining agents.** Read each fully, then add coherence sections.

**1. Add `needs` to YAML frontmatter:**
- maxsim-debugger: `needs: [phase_dir, state, config, conventions, codebase_docs]`
- maxsim-codebase-mapper: `needs: [codebase_docs]`
- maxsim-integration-checker: `needs: [phase_dir, state, requirements, codebase_docs]`

**2-7. Add system map, upstream_input, downstream_consumer, input_validation, deferred_items, minimum handoff contract:**

- **maxsim-debugger:** Receives from /maxsim:debug orchestrator or diagnose-issues workflow. Inputs: bug description or symptom report (inline, required), debug session file path (optional, for resuming). Produces: debug session file (file, durable), root cause analysis (inline, ephemeral). Validation: block if no bug description provided.

- **maxsim-codebase-mapper:** Receives from map-codebase orchestrator. Inputs: project directory (implicit from cwd). Produces: .planning/codebase/*.md files (file, durable) -- STACK.md, ARCHITECTURE.md, CONVENTIONS.md, STRUCTURE.md, CONCERNS.md. Validation: block if no source code found in project.

- **maxsim-integration-checker:** Receives from verify-work or execute-phase orchestrator. Inputs: phase directory (required), component boundaries to check (optional). Produces: integration report (inline, ephemeral). Validation: block if phase directory missing.

**Part B: Update AGENTS.md registry.**

Add a new section to AGENTS.md called `## Agent Coherence Conventions` with:

1. **System Map Maintenance:** "When adding a new agent, update the `<agent_system_map>` table in ALL existing agent prompts. The map is ~15 lines and inlined in each agent for zero-latency access."

2. **Required Sections:** "Every agent prompt MUST have these sections in order: frontmatter (with `needs`), agent_system_map, role, upstream_input, downstream_consumer, input_validation, ...agent-specific sections..., deferred_items, structured_returns (with minimum handoff contract)."

3. **Needs Vocabulary:** Document the standardized needs keys (phase_dir, roadmap, state, requirements, config, conventions, codebase_docs, project, inline) and what each maps to.

4. **Handoff Contract:** "Every agent structured return MUST include: Key Decisions, Artifacts, Status, Deferred Items."

Do NOT restructure or rewrite existing AGENTS.md content. ADD the new section at the end of the file.
</action>
<verify>
<automated>cd C:/Development/cli/maxsim && for f in templates/agents/maxsim-debugger.md templates/agents/maxsim-codebase-mapper.md templates/agents/maxsim-integration-checker.md; do echo "=== $f ===" && grep -c "agent_system_map" "$f" && grep -c "upstream_input" "$f" && grep -c "downstream_consumer" "$f" && grep -c "input_validation" "$f" && grep -c "deferred_items" "$f" && grep -c "needs:" "$f"; done && echo "=== AGENTS.md ===" && grep -c "Coherence Conventions\|System Map Maintenance\|Needs Vocabulary\|Handoff Contract" templates/agents/AGENTS.md</automated>
</verify>
<done>
- All 3 agents have system map, upstream_input, downstream_consumer, input_validation, deferred_items, needs frontmatter
- AGENTS.md has new "Agent Coherence Conventions" section with system map maintenance, required sections, needs vocabulary, and handoff contract documentation
- No existing AGENTS.md content was removed
</done>
</task>

</tasks>

<verification>
After both tasks complete, verify all 6 support agents plus AGENTS.md:

```bash
cd C:/Development/cli/maxsim
echo "--- Section counts per agent ---"
for f in templates/agents/maxsim-verifier.md templates/agents/maxsim-spec-reviewer.md templates/agents/maxsim-code-reviewer.md templates/agents/maxsim-debugger.md templates/agents/maxsim-codebase-mapper.md templates/agents/maxsim-integration-checker.md; do
  name=$(basename "$f")
  sys=$(grep -c "agent_system_map" "$f")
  up=$(grep -c "upstream_input" "$f")
  down=$(grep -c "downstream_consumer" "$f")
  val=$(grep -c "input_validation" "$f")
  def=$(grep -c "deferred_items" "$f")
  needs=$(grep -c "^needs:" "$f")
  echo "$name: sys=$sys up=$up down=$down val=$val def=$def needs=$needs"
done
echo "--- AGENTS.md conventions ---"
grep -c "Coherence Conventions" templates/agents/AGENTS.md
```

All counts should be >= 1.
</verification>

<success_criteria>
- [ ] All 6 support agent files have `needs` array in YAML frontmatter
- [ ] All 6 support agent files have `<agent_system_map>` with exact 13-agent table
- [ ] All 6 support agent files have `<upstream_input>` and `<downstream_consumer>` sections
- [ ] All 6 support agent files have `<input_validation>` with hard blocking
- [ ] All 6 support agent files have `<deferred_items>` protocol
- [ ] All 6 support agent files have minimum handoff contract in structured returns
- [ ] spec-reviewer and code-reviewer document frontmatter output format in downstream_consumer
- [ ] AGENTS.md has "Agent Coherence Conventions" section documenting system map maintenance, required sections, needs vocabulary, and handoff contract
- [ ] No existing agent functionality was removed or broken
</success_criteria>

<output>
SUMMARY.md in `.planning/phases/03-Agent-Coherence/` documenting which agents were updated and what sections were added.
</output>
