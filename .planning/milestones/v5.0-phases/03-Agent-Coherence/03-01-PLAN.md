---
phase: 03-Agent-Coherence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/agents/maxsim-executor.md
  - templates/agents/maxsim-planner.md
  - templates/agents/maxsim-plan-checker.md
  - templates/agents/maxsim-phase-researcher.md
  - templates/agents/maxsim-project-researcher.md
  - templates/agents/maxsim-research-synthesizer.md
  - templates/agents/maxsim-roadmapper.md
autonomous: true
requirements:
  - AGENT-01
  - AGENT-04
must_haves:
  truths:
    - "Each of the 7 core agents has a system map listing all 13 agents"
    - "Each of the 7 core agents has upstream_input and downstream_consumer sections"
    - "Each of the 7 core agents validates required inputs and returns structured error on missing"
    - "Each of the 7 core agents has deferred items protocol section"
    - "Each of the 7 core agents' structured returns include minimum handoff contract (decisions, artifacts, status, deferred items)"
    - "Each of the 7 core agents has needs field in YAML frontmatter"
  artifacts:
    - path: "templates/agents/maxsim-executor.md"
      provides: "Executor agent with system map, contracts, validation, deferred items, needs frontmatter"
      min_lines: 350
    - path: "templates/agents/maxsim-planner.md"
      provides: "Planner agent with system map, contracts, validation, deferred items, needs frontmatter"
      min_lines: 350
    - path: "templates/agents/maxsim-plan-checker.md"
      provides: "Plan checker agent with system map, contracts, validation, deferred items, needs frontmatter"
      min_lines: 100
    - path: "templates/agents/maxsim-phase-researcher.md"
      provides: "Phase researcher agent with system map, contracts, validation, deferred items, needs frontmatter"
      min_lines: 100
    - path: "templates/agents/maxsim-project-researcher.md"
      provides: "Project researcher agent with system map, contracts, validation, deferred items, needs frontmatter"
      min_lines: 100
    - path: "templates/agents/maxsim-research-synthesizer.md"
      provides: "Research synthesizer agent with system map, contracts, validation, deferred items, needs frontmatter"
      min_lines: 100
    - path: "templates/agents/maxsim-roadmapper.md"
      provides: "Roadmapper agent with system map, contracts, validation, deferred items, needs frontmatter"
      min_lines: 100
  key_links:
    - from: "templates/agents/maxsim-executor.md"
      to: "templates/agents/maxsim-spec-reviewer.md"
      via: "upstream_input/downstream_consumer handoff contract"
      pattern: "maxsim-spec-reviewer|maxsim-code-reviewer"
    - from: "templates/agents/maxsim-planner.md"
      to: "templates/agents/maxsim-phase-researcher.md"
      via: "upstream_input references RESEARCH.md format"
      pattern: "maxsim-phase-researcher|RESEARCH.md"
    - from: "templates/agents/maxsim-plan-checker.md"
      to: "templates/agents/maxsim-planner.md"
      via: "upstream_input references PLAN.md format"
      pattern: "maxsim-planner|PLAN.md"
---

<objective>
Add agent coherence sections to the 7 core agents: executor, planner, plan-checker, phase-researcher, project-researcher, research-synthesizer, and roadmapper. Each agent gets: (1) `needs` frontmatter field, (2) system map listing all 13 agents, (3) `<upstream_input>` section documenting what it receives and from whom, (4) `<downstream_consumer>` section documenting what it produces and for whom, (5) `<input_validation>` section with hard blocking on missing critical inputs, (6) `<deferred_items>` protocol section, and (7) minimum handoff contract in `<structured_returns>`.
</objective>

<execution_context>
@.planning/phases/03-Agent-Coherence/03-CONTEXT.md
@.planning/phases/03-Agent-Coherence/03-RESEARCH.md
</execution_context>

<context>
This plan is part of Phase 3: Agent Coherence. The goal is transforming 13 isolated agent prompts into a coordinated system with explicit handoff contracts.

**Key references from RESEARCH.md:**
- Pattern 1: Agent Prompt Section Ordering (defines where new sections go)
- Pattern 5: Minimum Handoff Contract format
- Code Example 1: System map table (exact 13-agent listing)
- Code Example 6: Input validation block template
- Pattern 3: Frontmatter `needs` vocabulary (standardized keys)

**Section ordering (from RESEARCH.md Pattern 1):**
1. YAML frontmatter (add `needs` field)
2. `<agent_system_map>` (NEW - right after frontmatter, before `<role>`)
3. `<role>` (existing)
4. `<upstream_input>` (NEW - after role)
5. `<downstream_consumer>` (NEW - after upstream_input)
6. ...existing agent-specific sections...
7. `<deferred_items>` (NEW - near end, before structured_returns)
8. `<structured_returns>` (existing - update to include handoff contract)

**System map (identical for all agents):**
```markdown
<agent_system_map>
## Agent System Map

| Agent | Role |
|-------|------|
| maxsim-executor | Implements plan tasks with atomic commits and deviation handling |
| maxsim-planner | Creates executable phase plans with goal-backward verification |
| maxsim-plan-checker | Verifies plans achieve phase goal before execution |
| maxsim-phase-researcher | Researches phase domain for planning context |
| maxsim-project-researcher | Researches project ecosystem during init |
| maxsim-research-synthesizer | Synthesizes parallel research into unified findings |
| maxsim-roadmapper | Creates roadmaps with phase breakdown and requirement mapping |
| maxsim-verifier | Verifies phase goal achievement with fresh evidence |
| maxsim-spec-reviewer | Reviews implementation for spec compliance |
| maxsim-code-reviewer | Reviews implementation for code quality |
| maxsim-debugger | Investigates bugs via systematic hypothesis testing |
| maxsim-codebase-mapper | Maps codebase structure and conventions |
| maxsim-integration-checker | Validates cross-component integration |
</agent_system_map>
```

**Deferred items protocol (identical for all agents):**
```markdown
<deferred_items>
## Deferred Items Protocol
When encountering work outside current scope:
1. DO NOT implement it
2. Add to output under `### Deferred Items`
3. Format: `- [{category}] {description} -- {why deferred}`
Categories: feature, bug, refactor, investigation
</deferred_items>
```

**Minimum handoff contract (in structured_returns):**
Every structured return must include these 4 sections:
- `### Key Decisions` - decisions made during execution
- `### Artifacts` - files created/modified
- `### Status` - complete/blocked/partial
- `### Deferred Items` - out-of-scope work encountered (or "None")

**Locked decisions (NON-NEGOTIABLE):**
- System map inlined in every agent (NOT a shared partial file)
- `<upstream_input>` references template files for format examples (e.g., "See templates/research.md for RESEARCH.md format") -- zero prompt bloat
- Hard validation blocks execution on missing critical input
- `needs` field as array of strings in frontmatter
</context>

<tasks>

<task id="1" type="auto">
<name>Add coherence sections to executor, planner, plan-checker, phase-researcher</name>
<files>
- templates/agents/maxsim-executor.md
- templates/agents/maxsim-planner.md
- templates/agents/maxsim-plan-checker.md
- templates/agents/maxsim-phase-researcher.md
</files>
<action>
For each of the 4 agent files, add the following sections in order. Read each file fully before modifying.

**1. Add `needs` to YAML frontmatter:**
- maxsim-executor: `needs: [phase_dir, state, config, conventions, codebase_docs]`
- maxsim-planner: `needs: [phase_dir, roadmap, state, requirements, config, codebase_docs]`
- maxsim-plan-checker: `needs: [phase_dir, roadmap, requirements, codebase_docs]`
- maxsim-phase-researcher: `needs: [phase_dir, roadmap, state, requirements, config, codebase_docs]`

**2. Add `<agent_system_map>` AFTER the closing `---` of frontmatter and BEFORE `<role>`:**
Use the exact 13-agent system map table from the context section above.

**3. Add `<upstream_input>` AFTER `</role>`:**
Each agent's upstream_input is unique. Document:
- **maxsim-executor:** Receives from execute-phase orchestrator. Inputs: PLAN.md file path (required), STATE.md (required), config.json (optional). Reference: "See plan frontmatter schema in packages/cli/src/core/frontmatter.ts for PLAN.md format". Validation: block if PLAN.md path missing or file not found.
- **maxsim-planner:** Receives from plan-phase orchestrator. Inputs: ROADMAP.md (required), REQUIREMENTS.md (required), CONTEXT.md from discuss-phase (optional), RESEARCH.md from research-phase (optional). Reference: "See .planning/ROADMAP.md for phase structure format". Validation: block if ROADMAP.md missing.
- **maxsim-plan-checker:** Receives from plan-phase orchestrator. Inputs: PLAN.md file(s) (required), ROADMAP.md (required), REQUIREMENTS.md (required). Reference: "See plan frontmatter schema for PLAN.md format". Validation: block if no PLAN.md files provided.
- **maxsim-phase-researcher:** Receives from plan-phase or research-phase orchestrator. Inputs: phase number (required), CONTEXT.md with locked decisions (optional), ROADMAP.md (required). Reference: "See 03-CONTEXT.md for CONTEXT.md format example". Validation: block if phase number missing.

**4. Add `<downstream_consumer>` AFTER `</upstream_input>`:**
- **maxsim-executor:** Produces for execute-phase orchestrator. Outputs: SUMMARY.md (file, durable), STATE.md updates (file, durable), git commits (durable). Contains: completion status, files created/modified, deviations, review cycle results.
- **maxsim-planner:** Produces for plan-phase orchestrator (then maxsim-plan-checker, then maxsim-executor). Outputs: PLAN.md file(s) (file, durable). Contains: tasks, must_haves, verification criteria.
- **maxsim-plan-checker:** Produces for plan-phase orchestrator (inline). Outputs: checker verdict (inline, ephemeral). Contains: pass/fail per dimension, fix hints for failures.
- **maxsim-phase-researcher:** Produces for maxsim-planner (via file). Outputs: RESEARCH.md (file, durable). Contains: standard stack, architecture patterns, pitfalls, code examples.

**5. Add `<input_validation>` section AFTER `</downstream_consumer>`:**
Use the template from Code Example 6 in RESEARCH.md. Customize the required inputs list per agent (matching the upstream_input required fields). Include the structured error return format with agent name, missing items, and expected source.

**6. Add `<deferred_items>` section near the end of each file, BEFORE `<structured_returns>` if it exists, or before `</output>` / end of file:**
Use the identical deferred items protocol block from context above.

**7. Update `<structured_returns>` (or add if missing):**
Ensure every structured return section includes the minimum handoff contract: Key Decisions, Artifacts, Status, Deferred Items. If the agent already has a structured_returns section, ADD the missing contract fields to it. Do NOT remove existing content.

IMPORTANT: Do NOT change any existing functional content in the agents. Only ADD new sections in the correct positions and UPDATE structured_returns. Preserve all existing sections, their content, and their ordering relative to each other.
</action>
<verify>
<automated>cd C:/Development/cli/maxsim && for f in templates/agents/maxsim-executor.md templates/agents/maxsim-planner.md templates/agents/maxsim-plan-checker.md templates/agents/maxsim-phase-researcher.md; do echo "=== $f ===" && grep -c "agent_system_map" "$f" && grep -c "upstream_input" "$f" && grep -c "downstream_consumer" "$f" && grep -c "input_validation" "$f" && grep -c "deferred_items" "$f" && grep -c "needs:" "$f" && grep -c "Key Decisions" "$f"; done</automated>
</verify>
<done>
- All 4 agent files contain: agent_system_map section, upstream_input section, downstream_consumer section, input_validation section, deferred_items section
- All 4 agent files have `needs:` in their YAML frontmatter
- All 4 agent files' structured returns include minimum handoff contract fields (Key Decisions, Artifacts, Status, Deferred Items)
- No existing content was removed or reordered
</done>
</task>

<task id="2" type="auto">
<files>
<name>Add coherence sections to project-researcher, research-synthesizer, roadmapper</name>
- templates/agents/maxsim-project-researcher.md
- templates/agents/maxsim-research-synthesizer.md
- templates/agents/maxsim-roadmapper.md
</files>
<action>
Apply the same treatment as Task 1 to the remaining 3 core agents. Read each file fully before modifying.

**1. Add `needs` to YAML frontmatter:**
- maxsim-project-researcher: `needs: [project, config, codebase_docs]`
- maxsim-research-synthesizer: `needs: [phase_dir, requirements, codebase_docs]`
- maxsim-roadmapper: `needs: [project, roadmap, requirements, state, codebase_docs]`

**2. Add `<agent_system_map>` AFTER frontmatter, BEFORE `<role>`:**
Same 13-agent table as Task 1.

**3. Add `<upstream_input>` AFTER `</role>`:**
- **maxsim-project-researcher:** Receives from init-new-project or init-existing orchestrator. Inputs: PROJECT.md draft (required), user tech stack preferences (inline, required). Reference: "See .planning/PROJECT.md for project document format". Validation: block if no project context provided.
- **maxsim-research-synthesizer:** Receives from research-phase orchestrator. Inputs: multiple RESEARCH.md fragments from parallel researchers (inline, required), REQUIREMENTS.md (optional). Reference: "See 03-RESEARCH.md for research output format". Validation: block if no research fragments provided.
- **maxsim-roadmapper:** Receives from new-project or new-milestone orchestrator. Inputs: PROJECT.md (required), REQUIREMENTS.md (required), previous ROADMAP.md (optional for new milestones). Reference: "See .planning/ROADMAP.md for roadmap format". Validation: block if PROJECT.md or REQUIREMENTS.md missing.

**4. Add `<downstream_consumer>` AFTER `</upstream_input>`:**
- **maxsim-project-researcher:** Produces for maxsim-research-synthesizer (via inline handoff). Outputs: research findings (inline, ephemeral). Contains: library analysis, trade-offs, recommendations.
- **maxsim-research-synthesizer:** Produces for init orchestrator (then written to PROJECT.md enrichment). Outputs: synthesized research (inline, ephemeral). Contains: unified tech stack recommendations, resolved conflicts between researchers.
- **maxsim-roadmapper:** Produces for new-project/new-milestone orchestrator (via file). Outputs: ROADMAP.md (file, durable), REQUIREMENTS.md (file, durable). Contains: phase breakdown, requirement mapping, dependency graph.

**5. Add `<input_validation>`, `<deferred_items>`, and update `<structured_returns>` using the same patterns as Task 1.**

Same rules: do NOT change existing content. Only add new sections in correct positions. Preserve existing section ordering.
</action>
<verify>
<automated>cd C:/Development/cli/maxsim && for f in templates/agents/maxsim-project-researcher.md templates/agents/maxsim-research-synthesizer.md templates/agents/maxsim-roadmapper.md; do echo "=== $f ===" && grep -c "agent_system_map" "$f" && grep -c "upstream_input" "$f" && grep -c "downstream_consumer" "$f" && grep -c "input_validation" "$f" && grep -c "deferred_items" "$f" && grep -c "needs:" "$f" && grep -c "Key Decisions" "$f"; done</automated>
</verify>
<done>
- All 3 agent files contain: agent_system_map, upstream_input, downstream_consumer, input_validation, deferred_items sections
- All 3 agent files have `needs:` in YAML frontmatter
- All 3 agent files' structured returns include minimum handoff contract
- No existing content was removed or reordered
</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full set of 7 agents:

```bash
cd C:/Development/cli/maxsim
echo "--- Section counts per agent ---"
for f in templates/agents/maxsim-executor.md templates/agents/maxsim-planner.md templates/agents/maxsim-plan-checker.md templates/agents/maxsim-phase-researcher.md templates/agents/maxsim-project-researcher.md templates/agents/maxsim-research-synthesizer.md templates/agents/maxsim-roadmapper.md; do
  name=$(basename "$f")
  sys=$(grep -c "agent_system_map" "$f")
  up=$(grep -c "upstream_input" "$f")
  down=$(grep -c "downstream_consumer" "$f")
  val=$(grep -c "input_validation" "$f")
  def=$(grep -c "deferred_items" "$f")
  needs=$(grep -c "^needs:" "$f")
  hand=$(grep -c "Key Decisions" "$f")
  echo "$name: sys=$sys up=$up down=$down val=$val def=$def needs=$needs handoff=$hand"
done
```

All counts should be >= 1 (typically 2 for opening+closing tags).
</verification>

<success_criteria>
- [ ] All 7 core agent files have `needs` array in YAML frontmatter
- [ ] All 7 core agent files have `<agent_system_map>` with exact 13-agent table
- [ ] All 7 core agent files have `<upstream_input>` with agent-specific input documentation
- [ ] All 7 core agent files have `<downstream_consumer>` with agent-specific output documentation
- [ ] All 7 core agent files have `<input_validation>` with hard blocking on missing critical inputs
- [ ] All 7 core agent files have `<deferred_items>` protocol section
- [ ] All 7 core agent files have minimum handoff contract in structured returns
- [ ] No existing agent functionality was removed or broken
- [ ] Section ordering follows Pattern 1 from RESEARCH.md
</success_criteria>

<output>
SUMMARY.md in `.planning/phases/03-Agent-Coherence/` documenting which agents were updated and what sections were added.
</output>
