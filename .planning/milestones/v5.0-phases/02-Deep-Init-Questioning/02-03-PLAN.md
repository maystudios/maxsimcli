---
phase: 02-Deep-Init-Questioning
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - templates/workflows/new-project.md
  - templates/workflows/init-existing.md
  - packages/cli/src/core/init.ts
autonomous: true
requirements:
  - INIT-01
  - INIT-02
  - INIT-03
  - INIT-04
must_haves:
  truths:
    - "new-project.md workflow references the enhanced questioning.md with domain checklist and gate logic"
    - "new-project.md workflow includes agent dry-run validation as a final step"
    - "init-existing.md workflow includes stack preference questions after codebase scan"
    - "init-existing.md workflow includes convention confirmation step"
    - "init-existing.md workflow includes agent dry-run validation as a final step"
    - "Both workflows generate CONVENTIONS.md during document creation"
    - "init.ts includes conventions_path in context assembly for downstream agents"
  artifacts:
    - path: "templates/workflows/new-project.md"
      provides: "Enhanced new-project workflow with deep questioning, no-gos confirmation, CONVENTIONS.md generation, and dry-run validation"
      min_lines: 1250
    - path: "templates/workflows/init-existing.md"
      provides: "Enhanced init-existing workflow with stack preference questions, convention confirmation, and dry-run validation"
      min_lines: 1250
    - path: "packages/cli/src/core/init.ts"
      provides: "Context assembly with conventions_path for downstream agent contexts"
      min_lines: 791
  key_links:
    - from: "templates/workflows/new-project.md"
      to: "templates/references/questioning.md"
      via: "@file reference loading domain checklist"
      pattern: "@.*questioning\\.md"
    - from: "templates/workflows/new-project.md"
      to: "templates/templates/conventions.md"
      via: "Template used during CONVENTIONS.md generation"
      pattern: "conventions"
    - from: "templates/workflows/init-existing.md"
      to: "templates/references/questioning.md"
      via: "@file reference loading domain checklist"
      pattern: "@.*questioning\\.md"
    - from: "packages/cli/src/core/init.ts"
      to: ".planning/CONVENTIONS.md"
      via: "conventions_path in context object"
      pattern: "conventions"
---

<objective>
Wire the enhanced questioning, research outputs, templates, and dry-run validation into the new-project and init-existing workflow orchestration files, and update the CLI context assembly to include CONVENTIONS.md.
</objective>

<execution_context>
This plan integrates all Phase 2 building blocks (from Plans 01 and 02) into the actual workflow files that users interact with. The workflows are large files (~1220 lines each) with numbered steps. Changes are targeted insertions/modifications to specific steps, NOT full rewrites.

Key user decisions (LOCKED):
- Minimum 10 rounds, 80% gate, conversational style (questioning.md is already updated -- workflow just needs to reference it and add gate orchestration)
- No-gos confirmation step before writing NO-GOS.md
- Init-existing: stack preference questions after scan, convention confirmation from scan-detected conventions
- CONVENTIONS.md generated during init (greenfield from research, brownfield from scan)
- Agent dry-run validation (always runs) after every init
- Dry-run prompt must say "Do NOT infer missing information" to prevent false passes
- Research synthesizer approval gate for locked decisions
- Init-existing does NOT get a separate research step (scan IS the research for brownfield)

Reference Plan 01 SUMMARY for: questioning.md structure, no-gos.md template structure
Reference Plan 02 SUMMARY for: conventions.md template structure, project.md Tech Stack Decisions section
</execution_context>

<context>
@templates/workflows/new-project.md (current 1225-line workflow -- targeted step modifications)
@templates/workflows/init-existing.md (current 1220-line workflow -- targeted step modifications)
@packages/cli/src/core/init.ts (current 791-line context assembly -- add conventions_path)
@.planning/phases/02-Deep-Init-Questioning/02-01-SUMMARY.md (what was built in Plan 01)
@.planning/phases/02-Deep-Init-Questioning/02-02-SUMMARY.md (what was built in Plan 02)
@.planning/phases/02-Deep-Init-Questioning/02-RESEARCH.md (Pattern 6: Agent Dry-Run, Pattern 7: Stack Preference)
</context>

<tasks>

<task id="01" type="auto">
<files>
- templates/workflows/new-project.md
- templates/workflows/init-existing.md
</files>
<action>
Read both workflow files completely before making changes. These are large (1200+ line) files with numbered steps. Make targeted modifications to specific steps.

**For `new-project.md`:**

1. **Modify the questioning step** (find the step that references questioning.md / thinking-partner.md):
   - Ensure it references the updated `@./references/questioning.md` which now includes domain checklist + no-gos tracking
   - Add orchestration for the gate: after each user response, the AI checks round count and domain coverage internally. The "Ready to create PROJECT.md?" option only appears when both conditions are met (round >= 10, coverage >= 80%).
   - Add instruction: before showing the "Ready?" option, display a coverage summary showing domains by category with COVERED/N-A/UNCOVERED status.
   - Add instruction: after user selects "Ready?", present accumulated no-gos for confirmation before writing NO-GOS.md.

2. **Modify the research step** (find the step that spawns project researchers):
   - Add instruction that the research synthesizer must produce a "Locked Decisions" section (the agent prompt is already updated -- the workflow just needs to reference the approval gate)
   - After synthesis completes, add an approval gate: present locked decisions to user, user can override any decision
   - After approval, add instruction to update PROJECT.md with the "Tech Stack Decisions" section

3. **Add CONVENTIONS.md generation** to the document creation step:
   - After research (if research ran): generate CONVENTIONS.md from research recommendations + questioning confirmations using the conventions.md template
   - If no research: generate CONVENTIONS.md from questioning context with reasonable defaults
   - Use the `artefakte-write` CLI command or direct template filling (consistent with how other documents are generated in the workflow)

4. **Add Agent Dry-Run Validation** as the FINAL step (new step, after all documents are generated):
   - Spawn a test agent using the Task tool with this prompt (from 02-RESEARCH.md Pattern 6):
     "You are a fresh agent about to start Phase 1 of this project. Read the following files and report what you would need to ask before starting work. Do NOT infer missing information. If a specific library version is not stated, report it as a gap. If the error handling pattern is not described, report it as a gap. Your job is to find what is NOT written, not to demonstrate you could figure it out."
   - Files to read: .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/CONVENTIONS.md, .planning/NO-GOS.md, .planning/ROADMAP.md
   - Report format: DRY-RUN RESULT with Can Start (YES/NO), Gaps Found (list), Quality Score (1-10)
   - If gaps found: fill them by updating relevant docs before completing init
   - Use the planner model for the dry-run agent (cheap, sufficient for gap detection)

**For `init-existing.md`:**

1. **Add Stack Preference Questions** (new step, after codebase scan completes but before document generation):
   - Read `.planning/codebase/STACK.md` and extract architecturally significant dependencies
   - Filter to only framework-level choices: framework, database, ORM, state management, testing framework, build tools
   - Explicitly EXCLUDE: utility libraries, dev tools, type definitions
   - Cap at 8-10 items max to avoid overwhelming the user
   - Present to user: "Your codebase uses these key technologies. For each, do you want to keep, evolve, or replace?"
   - Capture decisions as constraints for CONVENTIONS.md and DECISIONS.md

2. **Add Convention Confirmation** (new step, after stack preference questions):
   - Read `.planning/codebase/CONVENTIONS.md` (from the codebase mapper agent)
   - Present scan-detected conventions to user: "Your codebase uses these patterns. Should new code follow them?"
   - User confirms each convention
   - Confirmed conventions are promoted to `.planning/CONVENTIONS.md` using the conventions.md template

3. **Add No-Gos from Scan** integration:
   - Read `.planning/codebase/CONCERNS.md` (if exists)
   - Auto-suggest findings as candidate no-gos: "Your codebase has X pattern -- should this be a no-go for new code?"
   - User confirms each
   - Add confirmed items to the no-gos list before writing NO-GOS.md

4. **Add Agent Dry-Run Validation** as the FINAL step (same as new-project -- identical dry-run prompt and report format)

Important: Do NOT add a separate research step to init-existing. The codebase scan IS the research for brownfield projects. The init-existing flow does NOT use the project-researcher or research-synthesizer agents.
</action>
<verify>
<automated>
cd C:/Development/cli/maxsim && node -e "
const fs = require('fs');
const np = fs.readFileSync('templates/workflows/new-project.md', 'utf8');
const ie = fs.readFileSync('templates/workflows/init-existing.md', 'utf8');
const checks = [
  // new-project checks
  ['NP: references questioning.md', np.includes('questioning.md')],
  ['NP: has coverage/gate logic', np.includes('80%') || np.includes('coverage')],
  ['NP: has no-gos confirmation', np.includes('no-go') || np.includes('NO-GOS') || np.includes('No-Go')],
  ['NP: has CONVENTIONS.md generation', np.includes('CONVENTIONS.md') || np.includes('conventions')],
  ['NP: has dry-run validation', np.includes('dry-run') || np.includes('Dry-Run') || np.includes('DRY-RUN')],
  ['NP: dry-run says do not infer', np.includes('Do NOT infer') || np.includes('do not infer')],
  ['NP: has approval gate for research', np.includes('approval') || np.includes('Approval') || np.includes('locked decision') || np.includes('Locked Decision')],
  ['NP: >= 1250 lines', np.split('\n').length >= 1250],
  // init-existing checks
  ['IE: has stack preference questions', ie.includes('stack preference') || ie.includes('Stack Preference') || ie.includes('keep, evolve, or replace') || ie.includes('keep.*evolve.*replace')],
  ['IE: has convention confirmation', ie.includes('convention confirmation') || ie.includes('Convention Confirmation') || ie.includes('conventions')],
  ['IE: has CONCERNS.md integration', ie.includes('CONCERNS.md') || ie.includes('concerns')],
  ['IE: has CONVENTIONS.md generation', ie.includes('CONVENTIONS.md')],
  ['IE: has dry-run validation', ie.includes('dry-run') || ie.includes('Dry-Run') || ie.includes('DRY-RUN')],
  ['IE: >= 1250 lines', ie.split('\n').length >= 1250],
  ['IE: no research-synthesizer agent', !ie.includes('maxsim-research-synthesizer') && !ie.includes('project-researcher')],
];
let pass = 0;
checks.forEach(([name, ok]) => { console.log(ok ? 'PASS' : 'FAIL', name); if (ok) pass++; });
console.log('\n' + pass + '/' + checks.length + ' checks passed');
if (pass < checks.length) process.exit(1);
"
</automated>
</verify>
<done>
- new-project.md questioning step references enhanced questioning.md with gate orchestration
- new-project.md has no-gos confirmation step before writing NO-GOS.md
- new-project.md research step includes approval gate for locked decisions
- new-project.md generates CONVENTIONS.md during document creation
- new-project.md has agent dry-run validation as final step
- init-existing.md has stack preference questions (keep/evolve/replace) after scan
- init-existing.md has convention confirmation from scan-detected conventions
- init-existing.md integrates CONCERNS.md findings as candidate no-gos
- init-existing.md generates CONVENTIONS.md from confirmed conventions
- init-existing.md has agent dry-run validation as final step
- init-existing.md does NOT add research-synthesizer or project-researcher agents
</done>
</task>

<task id="02" type="auto">
<files>
- packages/cli/src/core/init.ts
</files>
<action>
Read `packages/cli/src/core/init.ts` and add `conventions_path` to the context assembly so that downstream agents (planner, executor, etc.) can reference CONVENTIONS.md.

1. Read the file to understand how context paths are assembled (look for patterns like `project_path`, `requirements_path`, `nogos_path`, `state_path` etc. in the context objects).

2. Add `conventions_path` following the exact same pattern used for other `.planning/` files:
   - Define the path: `.planning/CONVENTIONS.md`
   - Add it to relevant context objects (look at where `nogos_path` or `requirements_path` are included -- conventions should be in the same set of contexts)
   - Only include if the file exists (follow the same existence-check pattern used for optional files like NO-GOS.md)

3. Ensure the TypeScript compiles cleanly after the change. The change should be minimal -- likely 3-5 lines added.

4. Do NOT modify any other core module. Do NOT add new exports unless the pattern requires it.

5. After modifying init.ts, run `npx tsc --noEmit` from packages/cli to verify compilation.
</action>
<verify>
<automated>cd C:/Development/cli/maxsim/packages/cli && npx tsc --noEmit 2>&1 | head -20</automated>
</verify>
<done>
- init.ts includes conventions_path in context assembly for downstream agents
- conventions_path follows the same pattern as other optional .planning/ file paths (existence-checked)
- TypeScript compiles without errors
- Change is minimal (3-5 lines, no structural changes)
</done>
</task>

</tasks>

<verification>
Task 01 verification is automated via content checks on both workflow files.
Task 02 verification is TypeScript compilation check.

After both tasks complete, run the full build to ensure nothing is broken:
```bash
cd C:/Development/cli/maxsim && npm run build
```
</verification>

<success_criteria>
- new-project.md workflow has deep questioning with gate logic, no-gos confirmation, CONVENTIONS.md generation, research approval gate, and dry-run validation
- init-existing.md workflow has stack preference questions, convention confirmation, CONCERNS.md no-gos integration, CONVENTIONS.md generation, and dry-run validation
- init.ts includes conventions_path in context assembly
- `npm run build` succeeds
- No existing workflow functionality is broken (all additions are to specific steps, preserving surrounding structure)
</success_criteria>

<output>
On completion, write SUMMARY to `.planning/phases/02-Deep-Init-Questioning/02-03-SUMMARY.md` documenting what was built, files changed, and key decisions.
</output>
