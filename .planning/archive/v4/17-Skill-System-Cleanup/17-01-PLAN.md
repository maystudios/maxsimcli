---
phase: 17-Skill-System-Cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/skills/simplify/ (renamed to templates/skills/maxsim-simplify/)
  - templates/skills/batch-worktree/ (renamed to templates/skills/maxsim-batch/)
  - packages/cli/src/install/shared.ts
  - templates/agents/AGENTS.md
  - templates/skills/using-maxsim/SKILL.md
  - templates/skills/code-review/SKILL.md
  - templates/skills/maxsim-simplify/SKILL.md
  - templates/skills/maxsim-batch/SKILL.md
  - templates/agents/maxsim-code-reviewer.md
  - templates/agents/maxsim-roadmapper.md
  - templates/agents/maxsim-executor.md
  - templates/agents/maxsim-planner.md
  - packages/cli/tests/unit/skills.test.ts
autonomous: true
requirements:
  - SKILL-07
  - SKILL-08
  - SKILL-09
must_haves:
  truths:
    - "simplify skill directory is renamed to maxsim-simplify — no collision with Claude Code /simplify"
    - "batch-worktree skill directory is renamed to maxsim-batch — no collision with Claude Code /batch"
    - "builtInSkills array lists all 11 skills from templates/skills/ using new names"
    - "AGENTS.md Skill Reference table lists all 11 skills using new names"
    - "using-maxsim Available Skills table lists all 11 skills using new names"
    - "code-review and maxsim-simplify have non-overlapping descriptions"
    - "maxsim-batch frontmatter threshold matches body (3-30)"
    - "Every skill is assigned to at least one agent in AGENTS.md Registry"
    - "A test fails if builtInSkills diverges from templates/skills/ contents"
  artifacts:
    - path: "templates/skills/maxsim-simplify/SKILL.md"
      provides: "Renamed simplify skill with updated name frontmatter"
    - path: "templates/skills/maxsim-batch/SKILL.md"
      provides: "Renamed batch-worktree skill with updated name frontmatter"
    - path: "packages/cli/src/install/shared.ts"
      provides: "builtInSkills array with all 11 skills including renamed ones"
    - path: "templates/agents/AGENTS.md"
      provides: "Complete agent-skill registry and skill reference table"
    - path: "templates/skills/using-maxsim/SKILL.md"
      provides: "Complete Available Skills table with all 11 skills"
    - path: "packages/cli/tests/unit/skills.test.ts"
      provides: "Sync validation test for builtInSkills vs templates/skills/"
  key_links:
    - from: "packages/cli/src/install/shared.ts"
      to: "templates/skills/*"
      via: "builtInSkills array entries must match directory names"
      pattern: "builtInSkills.*=.*\\["
    - from: "templates/agents/AGENTS.md"
      to: "templates/skills/*"
      via: "Skill Reference table entries must match skill directories"
      pattern: "skills/.*/"
    - from: "templates/agents/maxsim-executor.md"
      to: "templates/skills/maxsim-simplify/"
      via: "available_skills table references .skills/maxsim-simplify/SKILL.md"
      pattern: "maxsim-simplify/SKILL.md"
---

<objective>
Rename conflicting skills (simplify -> maxsim-simplify, batch-worktree -> maxsim-batch), reconcile all skill registration points to list all 11 skills with new names, fix overlapping descriptions, map unassigned skills to agents, and add a guard test that catches future divergence.
</objective>

<execution_context>
MAXSIM ships 11 skills in `templates/skills/` but two have names that collide with Claude Code built-in commands: `simplify` collides with `/simplify` and `batch-worktree` (referred to as `batch` in the roadmap) collides with `/batch`. Additionally, only 9 of 11 skills are registered in three places (builtInSkills array, AGENTS.md, using-maxsim), 4 skills are unmapped to any agent, and code-review/simplify have overlapping descriptions.

The ROADMAP success criteria explicitly require:
1. `simplify` renamed to `maxsim-simplify`
2. `batch` (batch-worktree) renamed to `maxsim-batch`
</execution_context>

<context>
@templates/skills/ (all 11 skill directories)
@packages/cli/src/install/shared.ts (builtInSkills array, line 18)
@templates/agents/AGENTS.md (agent-skill registry)
@templates/skills/using-maxsim/SKILL.md (Available Skills table)
@templates/skills/code-review/SKILL.md (description to clarify)
@templates/skills/simplify/SKILL.md (to be renamed to maxsim-simplify)
@templates/skills/batch-worktree/SKILL.md (to be renamed to maxsim-batch)
@templates/agents/maxsim-code-reviewer.md
@templates/agents/maxsim-roadmapper.md
@templates/agents/maxsim-executor.md
@templates/agents/maxsim-planner.md
@.planning/phases/17-Skill-System-Cleanup/17-RESEARCH.md (full findings)
</context>

<tasks>

<task id="1" type="auto">
<title>Rename conflicting skills and register all skills with fixed descriptions</title>
<files>
- templates/skills/simplify/ (rename entire directory to templates/skills/maxsim-simplify/)
- templates/skills/batch-worktree/ (rename entire directory to templates/skills/maxsim-batch/)
- templates/skills/maxsim-simplify/SKILL.md (update name frontmatter)
- templates/skills/maxsim-batch/SKILL.md (update name frontmatter)
- packages/cli/src/install/shared.ts
- templates/agents/AGENTS.md
- templates/skills/using-maxsim/SKILL.md
- templates/skills/code-review/SKILL.md
- templates/agents/maxsim-code-reviewer.md
- templates/agents/maxsim-roadmapper.md
- templates/agents/maxsim-executor.md
- templates/agents/maxsim-planner.md
</files>
<action>
**Step 0: Rename skill directories** (MUST be done first — everything else uses the new names).

Use `git mv` for both renames so git tracks the history:
```bash
cd C:/Development/cli/maxsim
git mv templates/skills/simplify templates/skills/maxsim-simplify
git mv templates/skills/batch-worktree templates/skills/maxsim-batch
```

**Step 1: Update renamed skill frontmatter.**

In `templates/skills/maxsim-simplify/SKILL.md`:
- Change `name: simplify` to `name: maxsim-simplify`
- Update `description` to emphasize this is a **maintainability optimization pass** (duplication, dead code, unnecessary complexity). It answers "Is this code as simple as it can be?" Used after implementation, before committing. Make clear this is NOT a correctness gate — that is code-review's job.

In `templates/skills/maxsim-batch/SKILL.md`:
- Change `name: batch-worktree` to `name: maxsim-batch`
- Fix the frontmatter description threshold from "5-30" to "3-30" to match the body content

**Step 2: Update builtInSkills array** in `packages/cli/src/install/shared.ts` line 18.
- Replace `'simplify'` with `'maxsim-simplify'`
- Replace `'batch-worktree'` with `'maxsim-batch'` (was missing — add it)
- Add `'sdd'` (was missing)
- Result must be 11 entries matching exactly the 11 directory names in `templates/skills/`

**Step 3: Clarify code-review description** in `templates/skills/code-review/SKILL.md`:
- Emphasize it is a **correctness gate** (security, interface contracts, error handling, test coverage). It answers "Is this code correct and safe?" Used at phase completion or before merge.
- The key distinction: code-review = PASS/FAIL gate on correctness; maxsim-simplify = optimization pass on maintainability.

**Step 4: Update AGENTS.md** in `templates/agents/AGENTS.md`:
- Update ALL references from `simplify` to `maxsim-simplify` and from `batch-worktree` to `maxsim-batch`
- Add `sdd` and `maxsim-batch` to the **Skill Reference** table (with directory and purpose)
- Update the **Registry** table to map unassigned skills to agents:
  - `maxsim-code-reviewer`: add `code-review` (its PRIMARY skill)
  - `maxsim-roadmapper`: add `roadmap-writing`, `brainstorming`
  - `maxsim-executor`: add `maxsim-simplify` (runs simplification after implementation)
  - `maxsim-planner`: add `brainstorming`
- `sdd` and `maxsim-batch` are orchestration strategies invoked by workflows, not agent behaviors. Note this in the Skill Reference table.

**Step 5: Update using-maxsim skill table** in `templates/skills/using-maxsim/SKILL.md`:
- Replace `simplify` with `maxsim-simplify` everywhere
- Add `sdd` and `maxsim-batch` to the "Available Skills" table
- Use updated descriptions

**Step 6: Update agent prompt files** — replace old skill names with new ones:
- `templates/agents/maxsim-code-reviewer.md`: Add `<available_skills>` section with `code-review` skill (trigger: "Always — primary skill") and `verification-before-completion`
- `templates/agents/maxsim-roadmapper.md`: Add `<available_skills>` section with `brainstorming` and `roadmap-writing`
- `templates/agents/maxsim-executor.md`: Update any reference from `.skills/simplify/` to `.skills/maxsim-simplify/`. Add `maxsim-simplify` to existing `<available_skills>` table (trigger: "After implementing a task, before committing")
- `templates/agents/maxsim-planner.md`: Update any reference from `.skills/simplify/` to `.skills/maxsim-simplify/`. Add `brainstorming` to existing `<available_skills>` table (trigger: "When exploring design approaches during task breakdown")

Follow the existing `<available_skills>` format used in maxsim-executor.md and maxsim-debugger.md (table with Skill, Read path, Trigger columns).

**IMPORTANT:** After all edits, do a broad search for any remaining references to the OLD names (`simplify/SKILL.md`, `batch-worktree/SKILL.md`, `skills/simplify/`, `skills/batch-worktree/`) in the entire `templates/` directory and fix them. Also search `packages/cli/src/` for any references.
</action>
<verify>
<automated>cd C:/Development/cli/maxsim && node -e "
const fs = require('fs');
const path = require('path');

// Check directories were renamed
if (fs.existsSync('templates/skills/simplify')) { console.error('FAIL: templates/skills/simplify still exists'); process.exit(1); }
if (fs.existsSync('templates/skills/batch-worktree')) { console.error('FAIL: templates/skills/batch-worktree still exists'); process.exit(1); }
if (!fs.existsSync('templates/skills/maxsim-simplify/SKILL.md')) { console.error('FAIL: templates/skills/maxsim-simplify/SKILL.md missing'); process.exit(1); }
if (!fs.existsSync('templates/skills/maxsim-batch/SKILL.md')) { console.error('FAIL: templates/skills/maxsim-batch/SKILL.md missing'); process.exit(1); }
console.log('OK: directories renamed');

// Check frontmatter names updated
const simplifySkill = fs.readFileSync('templates/skills/maxsim-simplify/SKILL.md', 'utf-8');
const simplifyFm = simplifySkill.split('---')[1];
if (!simplifyFm.includes('name: maxsim-simplify')) { console.error('FAIL: maxsim-simplify frontmatter name not updated'); process.exit(1); }
console.log('OK: maxsim-simplify name updated');

const batchSkill = fs.readFileSync('templates/skills/maxsim-batch/SKILL.md', 'utf-8');
const batchFm = batchSkill.split('---')[1];
if (!batchFm.includes('name: maxsim-batch')) { console.error('FAIL: maxsim-batch frontmatter name not updated'); process.exit(1); }
if (batchFm.includes('5-30')) { console.error('FAIL: maxsim-batch frontmatter still says 5-30'); process.exit(1); }
if (!batchFm.includes('3-30')) { console.error('FAIL: maxsim-batch frontmatter does not say 3-30'); process.exit(1); }
console.log('OK: maxsim-batch name and threshold updated');

// Check builtInSkills has 11 entries matching directories
const shared = fs.readFileSync('packages/cli/src/install/shared.ts', 'utf-8');
const match = shared.match(/builtInSkills\s*=\s*\[([\s\S]*?)\]/);
if (!match) { console.error('FAIL: builtInSkills not found'); process.exit(1); }
const skills = match[1].match(/'([^']+)'/g).map(s => s.replace(/'/g, ''));
const dirs = fs.readdirSync('templates/skills');
const missing = dirs.filter(d => !skills.includes(d));
const extra = skills.filter(s => !dirs.includes(s));
if (missing.length) { console.error('FAIL: missing from builtInSkills:', missing); process.exit(1); }
if (extra.length) { console.error('FAIL: extra in builtInSkills:', extra); process.exit(1); }
if (skills.includes('simplify')) { console.error('FAIL: builtInSkills still contains old name simplify'); process.exit(1); }
if (skills.includes('batch-worktree')) { console.error('FAIL: builtInSkills still contains old name batch-worktree'); process.exit(1); }
console.log('OK: builtInSkills has all', skills.length, 'skills with new names');

// Check AGENTS.md has all 11 with new names
const agents = fs.readFileSync('templates/agents/AGENTS.md', 'utf-8');
const missingInAgents = dirs.filter(d => !agents.includes(d));
if (missingInAgents.length) { console.error('FAIL: missing from AGENTS.md:', missingInAgents); process.exit(1); }
if (agents.includes('skills/simplify/')) { console.error('FAIL: AGENTS.md still references old simplify path'); process.exit(1); }
if (agents.includes('skills/batch-worktree/')) { console.error('FAIL: AGENTS.md still references old batch-worktree path'); process.exit(1); }
console.log('OK: AGENTS.md references all skills with new names');

// Check using-maxsim has all 11 with new names
const usingMaxsim = fs.readFileSync('templates/skills/using-maxsim/SKILL.md', 'utf-8');
const missingInUsing = dirs.filter(d => !usingMaxsim.includes(d));
if (missingInUsing.length) { console.error('FAIL: missing from using-maxsim:', missingInUsing); process.exit(1); }
console.log('OK: using-maxsim references all skills');

// Check code-review agent has code-review skill
const reviewer = fs.readFileSync('templates/agents/maxsim-code-reviewer.md', 'utf-8');
if (!reviewer.includes('code-review/SKILL.md')) { console.error('FAIL: maxsim-code-reviewer does not reference code-review skill'); process.exit(1); }
console.log('OK: maxsim-code-reviewer references code-review skill');

// Check executor references maxsim-simplify (not simplify)
const executor = fs.readFileSync('templates/agents/maxsim-executor.md', 'utf-8');
if (executor.includes('.skills/simplify/')) { console.error('FAIL: maxsim-executor still references old .skills/simplify/ path'); process.exit(1); }
if (!executor.includes('maxsim-simplify')) { console.error('FAIL: maxsim-executor does not reference maxsim-simplify'); process.exit(1); }
console.log('OK: maxsim-executor references maxsim-simplify');

// Check no stale references in templates/
const { execSync } = require('child_process');
try {
  const stale = execSync('grep -r \"skills/simplify/\" templates/ 2>/dev/null || true', { encoding: 'utf-8' }).trim();
  if (stale) { console.error('FAIL: stale simplify references in templates/:', stale); process.exit(1); }
  const stale2 = execSync('grep -r \"skills/batch-worktree/\" templates/ 2>/dev/null || true', { encoding: 'utf-8' }).trim();
  if (stale2) { console.error('FAIL: stale batch-worktree references in templates/:', stale2); process.exit(1); }
} catch(e) {}
console.log('OK: no stale old-name references in templates/');

console.log('ALL CHECKS PASSED');
"</automated>
</verify>
<done>
- templates/skills/simplify/ renamed to templates/skills/maxsim-simplify/ with name frontmatter updated
- templates/skills/batch-worktree/ renamed to templates/skills/maxsim-batch/ with name frontmatter updated and threshold fixed to 3-30
- builtInSkills array contains exactly 11 entries matching templates/skills/ directories (using new names, plus sdd added)
- AGENTS.md Skill Reference table lists all 11 skills with new names
- AGENTS.md Registry maps code-review to maxsim-code-reviewer, brainstorming+roadmap-writing to maxsim-roadmapper, maxsim-simplify to maxsim-executor, brainstorming to maxsim-planner
- using-maxsim Available Skills table lists all 11 skills with new names
- code-review description emphasizes correctness gate, maxsim-simplify description emphasizes maintainability optimization
- Agent prompt files updated with new skill names and available_skills sections
- No stale references to old names (simplify/, batch-worktree/) remain in templates/ or packages/cli/src/
</done>
</task>

<task id="2" type="auto">
<title>Add builtInSkills sync guard test</title>
<files>
- packages/cli/tests/unit/skills.test.ts
</files>
<action>
Add a new `describe` block to the existing `packages/cli/tests/unit/skills.test.ts` that validates the `builtInSkills` array stays in sync with `templates/skills/` directory contents.

**Test: "builtInSkills matches templates/skills/ directory contents"**

1. Import `builtInSkills` from `../../src/install/shared.js`
2. Read `templates/skills/` directory at the repo root (resolve from `__dirname` going up to repo root: `path.resolve(__dirname, '..', '..', '..', '..', 'templates', 'skills')`)
3. Get all subdirectory names from `templates/skills/`
4. Assert that `[...builtInSkills].sort()` equals the sorted directory list
5. Assert the count matches (currently 11)

This test will fail if someone adds a new skill directory without updating `builtInSkills`, or removes a skill without updating the array.

Note: `builtInSkills` is `as const` (readonly tuple), so spread it into a mutable array before sorting: `[...builtInSkills].sort()`.

**Also verify the renamed skills appear correctly:** add assertions that `builtInSkills` includes `'maxsim-simplify'` and `'maxsim-batch'` (and does NOT include the old names `'simplify'` or `'batch-worktree'`).
</action>
<verify>
<automated>cd C:/Development/cli/maxsim/packages/cli && npx vitest run tests/unit/skills.test.ts 2>&1 | tail -20</automated>
</verify>
<done>
- New test "builtInSkills matches templates/skills/ directory contents" exists and passes
- Test asserts maxsim-simplify and maxsim-batch are present (not old names)
- Test would fail if a skill is added to templates/skills/ without updating builtInSkills
- Test would fail if a skill is removed from templates/skills/ without updating builtInSkills
</done>
</task>

</tasks>

<verification>
After both tasks complete, run:

1. `node -e "..."` inline check (from Task 1 verify) confirms directories renamed, all 11 skills registered with new names, no stale references
2. `cd packages/cli && npx vitest run tests/unit/skills.test.ts` confirms the guard test passes
3. `npm run build` confirms no TypeScript/build errors from shared.ts change
</verification>

<success_criteria>
- [ ] `templates/skills/simplify/` renamed to `templates/skills/maxsim-simplify/` with `name: maxsim-simplify` in frontmatter
- [ ] `templates/skills/batch-worktree/` renamed to `templates/skills/maxsim-batch/` with `name: maxsim-batch` in frontmatter
- [ ] `builtInSkills` array in shared.ts contains exactly 11 entries matching templates/skills/ directories (new names)
- [ ] AGENTS.md lists all 11 skills in Skill Reference table and maps all skills to agents in Registry
- [ ] using-maxsim SKILL.md lists all 11 skills in Available Skills table
- [ ] code-review description says "correctness gate"; maxsim-simplify says "maintainability optimization"
- [ ] maxsim-batch frontmatter threshold is "3-30"
- [ ] maxsim-code-reviewer agent prompt references code-review skill in available_skills
- [ ] maxsim-roadmapper agent prompt references brainstorming and roadmap-writing skills
- [ ] No stale references to old names (simplify/, batch-worktree/) in templates/ or src/
- [ ] Guard test in skills.test.ts validates builtInSkills matches templates/skills/ and passes
- [ ] `npm run build` succeeds
</success_criteria>

<output>
Commit all changes. Update ROADMAP.md plan count for Phase 17.
</output>
