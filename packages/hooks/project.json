{
  "name": "hooks",
  "root": "packages/hooks",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsdown",
        "cwd": "packages/hooks"
      }
    }
  }
}
